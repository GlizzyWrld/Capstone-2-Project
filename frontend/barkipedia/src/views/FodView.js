import React from "react";
import { Fod } from "../components";

export function FodView() {
  return (
    <div>
      This will show a random fact and have a timer for next fact. also should
      have a subscription section for users
      <div>
      <Fod/>
      </div>
    </div>
  );
}
